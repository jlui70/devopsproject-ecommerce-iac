---
- name: Add KEDA Helm repository
  ansible.builtin.command:
    cmd: "helm repo add kedacore https://kedacore.github.io/charts"
  environment:
    KUBECONFIG: "{{ kubeconfig_path }}"
  changed_when: true

- name: Update Helm repositories
  ansible.builtin.command:
    cmd: "helm repo update"
  environment:
    KUBECONFIG: "{{ kubeconfig_path }}"
  changed_when: true

- name: Install or upgrade KEDA operator via Helm
  ansible.builtin.command:
    cmd: >
      helm upgrade --install keda kedacore/keda
      --namespace keda
      --create-namespace
      --version {{ keda_chart_version }}
      --timeout 120s
  environment:
    KUBECONFIG: "{{ kubeconfig_path }}"
  changed_when: true

- name: Wait for KEDA operator pod to be running
  ansible.builtin.command:
    cmd: >
      kubectl wait pod
      --namespace keda
      --selector=app=keda-operator
      --for=condition=Ready
      --timeout=120s
  environment:
    KUBECONFIG: "{{ kubeconfig_path }}"
  changed_when: false
  failed_when: false
